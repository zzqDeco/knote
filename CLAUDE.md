# CLAUDE.md

`knote` is implemented as a Go-first Bubble Tea application. The project intentionally avoids a TypeScript TUI and Node package manager in the MVP.

Use the local docs in `plan/` and `doc/` as implementation inputs. Treat leaked or third-party agent CLI source as product-shape reference only; do not copy code or private protocols.
