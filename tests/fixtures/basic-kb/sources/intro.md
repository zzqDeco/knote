# knote fixture

knote is a local-first knowledge workspace TUI.

It builds deterministic artifacts from local source files, queries evidence, and versions knowledge states with Git.

The MVP uses Go for the CLI and TUI, and delegates OpenSPG/KAG integration to a Python NDJSON adapter.
