//go:build darwin || linux

package connector

import "os"

func syncConnectorDirectory(path string) error {
	directory, err := os.Open(path)
	if err != nil {
		return err
	}
	defer directory.Close()
	return directory.Sync()
}
